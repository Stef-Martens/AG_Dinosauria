using UnityEngine;

public class TongueCurlCollision : MonoBehaviour
{
    //public BugCount BugCount;
    //public HeartCount HeartCount;

 /*   private void OnTriggerEnter(Collider other)
    {
        if (other.transform.parent != null)
        {
            if (other.transform.parent.gameObject.GetComponent<FlyV1>() || other.transform.parent.gameObject.GetComponent<FlyV2>())
            {
                other.transform.parent.gameObject.SetActive(false);
                //BugCount.BugsTaken += 1;
            }
            
            if (other.transform.parent.gameObject.GetComponent<HazardV1>() || other.transform.parent.gameObject.GetComponent<HazardV2>())
            {
                //HeartCount.HeartsCount -= 1;
            }
        }
    }*/
}